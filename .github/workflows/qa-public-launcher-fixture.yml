name: QA Fixture Pública do Launcher

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: qa-public-launcher-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    container:
      image: mcr.microsoft.com/playwright:v1.55.0-noble
    env:
      PREVIEW_URL: https://innov-git-fix-module-validation-loop-20260730-apex-method.vercel.app/amostra-launcher?_vercel_share=yTE1P8scAVaWnvBnYZOffBrFCIyka7j6
      QA_OUTPUT_DIR: artifacts/public-launcher-fixture
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Playwright runtime
        run: npm install --no-save --no-package-lock playwright@1.55.0

      - name: Capture public launcher fixture
        run: node scripts/run-public-launcher-fixture-qa.mjs

      - name: Upload screenshots and report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: public-launcher-fixture-${{ github.run_id }}
          path: artifacts/public-launcher-fixture/
          if-no-files-found: warn
          retention-days: 30
