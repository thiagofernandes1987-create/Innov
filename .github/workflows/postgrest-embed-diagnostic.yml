name: PostgREST Embed Diagnostic

on:
  pull_request:
    paths:
      - "app/**"
      - "lib/**"
      - "components/**"
      - "supabase/migrations/**"
      - "scripts/validate-postgrest-embeds.mjs"
      - "scripts/diagnose-rh-fk-ambiguity.mjs"
      - ".github/workflows/postgrest-embed-diagnostic.yml"
  push:
    branches: ["feature/**"]
    paths:
      - "app/**"
      - "lib/**"
      - "components/**"
      - "supabase/migrations/**"
      - "scripts/validate-postgrest-embeds.mjs"
      - "scripts/diagnose-rh-fk-ambiguity.mjs"
      - ".github/workflows/postgrest-embed-diagnostic.yml"

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - name: Run PostgREST embed validator
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          node scripts/validate-postgrest-embeds.mjs 2>&1 | tee postgrest-embeds-validation.txt
      - name: Diagnose redundant RH foreign keys in final PostgreSQL schema
        run: node scripts/diagnose-rh-fk-ambiguity.mjs
      - name: Upload diagnostic
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: postgrest-embeds-validation-${{ github.run_id }}
          path: |
            postgrest-embeds-validation.txt
            .evidence/rh/redundant-foreign-keys.txt
          if-no-files-found: error
          retention-days: 2
