apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
spec:
  selector:
    app.kubernetes.io/name: {{ .Release.Name }}-api
  ports:
    - name: http
      port: 80
      targetPort: http
