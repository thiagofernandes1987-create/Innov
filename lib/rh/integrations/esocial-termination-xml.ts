import{makeEsocialEventId,normalizeEmployerRegistrationNumber,type EsocialEnvironment}from"./esocial-xml";
const LAYOUT="v_S_01_03_00";const VER_PROC=process.env.ESOCIAL_PROCESS_VERSION?.trim()||"Innovar-RH/0.1";
function x(v:unknown){return String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&apos;");}function tag(n:string,v:unknown){return `<${n}>${x(v)}</${n}>`;}function opt(n:string,v:unknown){return v===null||v===undefined||v===""?"":tag(n,v);}function digits(v:string){return v.replace(/\D/g,"");}function tpAmb(e:EsocialEnvironment){return e==="PRODUCTION"?1:2;}function ideEvento(e:EsocialEnvironment,r?:string|null){return `<ideEvento>${tag("indRetif",r?2:1)}${r?tag("nrRecibo",r):""}${tag("tpAmb",tpAmb(e))}${tag("procEmi",1)}${tag("verProc",VER_PROC)}</ideEvento>`;}function ideEmpregador(t:1|2,n:string){return `<ideEmpregador>${tag("tpInsc",t)}${tag("nrInsc",normalizeEmployerRegistrationNumber(t,n))}</ideEmpregador>`;}
export type TerminationEsocialLine={lineCode:string;amount:number;rubricCode:string;tableCode:string;establishmentType:1|3|4;establishmentNumber:string;lotacaoCode:string;};
function verbas(lines:TerminationEsocialLine[],demonstrativeId:string){if(!lines.length)throw new Error("Evento rescisório exige ao menos uma verba mapeada.");const groups=new Map<string,TerminationEsocialLine[]>();for(const line of lines){if(!line.rubricCode||!line.tableCode||!line.lotacaoCode)throw new Error(`Linha ${line.lineCode} sem rubrica/tabela/lotação eSocial.`);const key=`${line.establishmentType}|${line.establishmentNumber}|${line.lotacaoCode}`;(groups.get(key)??groups.set(key,[]).get(key)!).push(line);}const perApur=[...groups.values()].map(group=>{const first=group[0];const det=group.map(l=>`<detVerbas>${tag("codRubr",l.rubricCode)}${tag("ideTabRubr",l.tableCode)}${tag("vrRubr",Math.abs(Number(l.amount)).toFixed(2))}</detVerbas>`).join("");return `<ideEstabLot>${tag("tpInsc",first.establishmentType)}${tag("nrInsc",digits(first.establishmentNumber))}${tag("codLotacao",first.lotacaoCode)}${det}</ideEstabLot>`;}).join("");return `<verbasResc><dmDev>${tag("ideDmDev",demonstrativeId)}<infoPerApur>${perApur}</infoPerApur></dmDev></verbasResc>`;}
export type S2299Input={environment:EsocialEnvironment;employerType:1|2;employerNumber:string;cpf:string;registrationNumber:string;reasonCode:string;terminationDate:string;noticeIndemnified:boolean;projectedNoticeEnd?:string|null;pensionPercentage?:number|null;pensionAmount?:number|null;deathCertificateNumber?:string|null;collectiveDismissalIndicator?:"S"|"N"|null;lines:TerminationEsocialLine[];demonstrativeId:string;receipt?:string|null;eventKey?:string;};
export function buildS2299(input:S2299Input){const eventKey=input.eventKey??makeEsocialEventId(input.employerType,input.employerNumber);if(!/^\d{11}$/.test(digits(input.cpf)))throw new Error("S-2299 exige CPF com 11 dígitos.");if(!/^\d{2}$/.test(input.reasonCode))throw new Error("S-2299 exige motivo de desligamento com 2 dígitos.");if(input.noticeIndemnified&&!input.projectedNoticeEnd)throw new Error("Aviso indenizado exige data projetada de fim.");const pens=input.pensionPercentage!=null&&input.pensionAmount!=null?3:input.pensionPercentage!=null?1:input.pensionAmount!=null?2:0;const info=`<infoDeslig>${tag("mtvDeslig",input.reasonCode)}${tag("dtDeslig",input.terminationDate)}${tag("indPagtoAPI",input.noticeIndemnified?"S":"N")}${input.noticeIndemnified?tag("dtProjFimAPI",input.projectedNoticeEnd):""}${tag("pensAlim",pens)}${pens===1||pens===3?tag("percAliment",Number(input.pensionPercentage).toFixed(2)):""}${pens===2||pens===3?tag("vrAlim",Number(input.pensionAmount).toFixed(2)):""}${opt("nrCertObito",input.deathCertificateNumber)}${tag("indPDV",input.collectiveDismissalIndicator??"N")}${verbas(input.lines,input.demonstrativeId)}</infoDeslig>`;return `<eSocial xmlns="http://www.esocial.gov.br/schema/evt/evtDeslig/${LAYOUT}"><evtDeslig Id="${x(eventKey)}">${ideEvento(input.environment,input.receipt)}${ideEmpregador(input.employerType,input.employerNumber)}<ideVinculo>${tag("cpfTrab",digits(input.cpf))}${tag("matricula",input.registrationNumber)}</ideVinculo>${info}</evtDeslig></eSocial>`;}
export type S2399Input={environment:EsocialEnvironment;employerType:1|2;employerNumber:string;cpf:string;categoryCode:string;registrationNumber?:string|null;terminationDate:string;reasonCode?:string|null;pensionPercentage?:number|null;pensionAmount?:number|null;lines:TerminationEsocialLine[];demonstrativeId:string;receipt?:string|null;eventKey?:string;};
export function buildS2399(input:S2399Input){const eventKey=input.eventKey??makeEsocialEventId(input.employerType,input.employerNumber);if(!/^\d{11}$/.test(digits(input.cpf)))throw new Error("S-2399 exige CPF com 11 dígitos.");if(!/^\d{3}$/.test(input.categoryCode))throw new Error("S-2399 exige categoria TSVE com 3 dígitos.");const pens=input.pensionPercentage!=null&&input.pensionAmount!=null?3:input.pensionPercentage!=null?1:input.pensionAmount!=null?2:0;const ide=`<ideTrabSemVinculo>${tag("cpfTrab",digits(input.cpf))}${tag("codCateg",input.categoryCode)}${opt("matricula",input.registrationNumber)}</ideTrabSemVinculo>`;const info=`<infoTSVTermino>${tag("dtTerm",input.terminationDate)}${opt("mtvDeslig",input.reasonCode)}${tag("pensAlim",pens)}${pens===1||pens===3?tag("percAliment",Number(input.pensionPercentage).toFixed(2)):""}${pens===2||pens===3?tag("vrAlim",Number(input.pensionAmount).toFixed(2)):""}${verbas(input.lines,input.demonstrativeId)}</infoTSVTermino>`;return `<eSocial xmlns="http://www.esocial.gov.br/schema/evt/evtTSVTermino/${LAYOUT}"><evtTSVTermino Id="${x(eventKey)}">${ideEvento(input.environment,input.receipt)}${ideEmpregador(input.employerType,input.employerNumber)}${ide}${info}</evtTSVTermino></eSocial>`;}
