import Link from"next/link";
import{RelationshipMetric}from"@/components/relationship/relationship-metric";
import{RelationshipNavigation}from"@/components/relationship/relationship-navigation";
import{formatRelationshipCurrency,formatRelationshipDateTime,LEAD_STAGE_LABELS,OPPORTUNITY_STAGE_LABELS,safeText}from"@/lib/relationship/domain";
import{loadCrmDashboard}from"@/lib/relationship/server";

export const dynamic="force-dynamic";

export default async function CrmDashboardPage(){
 const{pipeline,leads,opportunities,activities,errors}=await loadCrmDashboard();
 const openOpportunities=opportunities.filter(item=>!["WON","LOST"].includes(String(item.stage))).length;
 return <main className="content relationship-app"><section className="page-heading"><div><span className="badge">APLICATIVO · COMERCIAL</span><h1>CRM e Vendas</h1><p>Da primeira indicação ao fechamento: leads, oportunidades, atividades e relacionamento contínuo.</p></div><div className="page-actions"><Link className="button button-secondary" href="/app/crm/oportunidades/novo">Nova oportunidade</Link><Link className="button button-primary" href="/app/crm/leads/novo">Novo lead</Link></div></section><RelationshipNavigation/>{errors.length?<div className="validation blocking">{errors.join(" · ")}</div>:null}<section className="stats-grid relationship-stats"><RelationshipMetric label="LEADS ATIVOS" value={String(leads.filter(item=>!['CONVERTED','DISQUALIFIED'].includes(String(item.stage))).length)} detail={`${pipeline.followUpsDue} acompanhamento(s) vencido(s)`} tone={pipeline.followUpsDue>0?"warning":"ok"}/><RelationshipMetric label="OPORTUNIDADES" value={String(openOpportunities)} detail="em negociação comercial"/><RelationshipMetric label="PIPELINE PONDERADO" value={formatRelationshipCurrency(pipeline.weightedPipeline)} detail="valor × probabilidade"/><RelationshipMetric label="VENDAS GANHAS" value={formatRelationshipCurrency(pipeline.wonValue)} detail="oportunidades fechadas" tone="ok"/></section><section className="relationship-launch-grid"><Link className="card card-pad relationship-launch" href="/app/crm/leads"><span>◎</span><div><h2>Leads</h2><p>Origem, interesse, orçamento, responsável e próximo contato.</p></div></Link><Link className="card card-pad relationship-launch" href="/app/crm/oportunidades"><span>◈</span><div><h2>Funil comercial</h2><p>Prospecção, proposta, negociação, ganho e perda auditada.</p></div></Link><Link className="card card-pad relationship-launch" href="/app/clientes"><span>♙</span><div><h2>Cliente 360</h2><p>Contatos, oportunidades, contratos, obras e atendimentos.</p></div></Link><Link className="card card-pad relationship-launch" href="/app/ocorrencias"><span>◌</span><div><h2>SAC e pós-venda</h2><p>Chamados, SLA, anexos e satisfação do cliente.</p></div></Link></section><div className="relationship-two-column"><section className="card card-pad"><div className="section-heading"><div><span className="eyebrow">LEADS RECENTES</span><h2>Novas oportunidades</h2></div><Link className="text-link" href="/app/crm/leads">Ver todos →</Link></div><div className="relationship-list">{leads.slice(0,8).map(lead=><Link className="relationship-row" href={`/app/crm/leads/${lead.id}`} key={lead.id}><div><strong>{safeText(lead.full_name)}</strong><small>{safeText(lead.interest,"Interesse não informado")} · {safeText(lead.source,"Origem não informada")}</small></div><div><span className="badge">{LEAD_STAGE_LABELS[String(lead.stage)]??lead.stage}</span><small>{formatRelationshipDateTime(lead.next_follow_up_at)}</small></div></Link>)}{!leads.length?<div className="empty-state"><h3>Nenhum lead cadastrado</h3><p>Cadastre o primeiro contato comercial.</p></div>:null}</div></section><section className="card card-pad"><div className="section-heading"><div><span className="eyebrow">ATIVIDADES</span><h2>Próximos acompanhamentos</h2></div></div><div className="relationship-list">{activities.slice(0,8).map(activity=><div className="relationship-row" key={activity.id}><div><strong>{safeText(activity.subject)}</strong><small>{safeText(activity.activity_type)} · {safeText(activity.description,"")}</small></div><div><small>{formatRelationshipDateTime(activity.due_at??activity.occurred_at)}</small></div></div>)}{!activities.length?<div className="empty-state"><h3>Agenda em dia</h3><p>Nenhum acompanhamento pendente.</p></div>:null}</div></section></div><section className="card card-pad"><div className="section-heading"><div><span className="eyebrow">FUNIL</span><h2>Oportunidades recentes</h2></div><Link className="text-link" href="/app/crm/oportunidades">Abrir funil →</Link></div><div className="report-table-wrap"><table className="data-table"><thead><tr><th>Código</th><th>Oportunidade</th><th>Estágio</th><th>Probabilidade</th><th>Valor</th><th>Previsão</th></tr></thead><tbody>{opportunities.slice(0,12).map(opportunity=><tr key={opportunity.id}><td><Link href={`/app/crm/oportunidades/${opportunity.id}`}>{safeText(opportunity.code)}</Link></td><td>{safeText(opportunity.title)}</td><td><span className="badge">{OPPORTUNITY_STAGE_LABELS[String(opportunity.stage)]??opportunity.stage}</span></td><td>{Number(opportunity.probability)}%</td><td>{formatRelationshipCurrency(opportunity.estimated_value)}</td><td>{safeText(opportunity.expected_close_date)}</td></tr>)}</tbody></table>{!opportunities.length?<div className="empty-state"><h3>Sem oportunidades</h3><p>Converta um lead ou crie uma oportunidade diretamente.</p></div>:null}</div></section></main>;
}
