import Link from"next/link";
import{RelationshipNavigation}from"@/components/relationship/relationship-navigation";
import{formatRelationshipDateTime,safeText}from"@/lib/relationship/domain";
import{loadClientDirectory}from"@/lib/relationship/server";

export const dynamic="force-dynamic";
export default async function ClientsPage({searchParams}:{searchParams:Promise<{q?:string}>}){const query=await searchParams;const{clients}=await loadClientDirectory(query.q);return <main className="content relationship-app"><section className="page-heading"><div><span className="badge">APLICATIVO · CLIENTES</span><h1>Clientes</h1><p>Cadastro 360 com contatos, consentimentos, oportunidades, contratos, múltiplas obras e chamados.</p></div><Link className="button button-primary" href="/app/clientes/novo">Novo cliente</Link></section><RelationshipNavigation/><form className="relationship-search"><input name="q" defaultValue={query.q??""} placeholder="Buscar por nome, e-mail ou telefone"/><button className="button button-secondary" type="submit">Buscar</button></form><section className="card card-pad"><div className="report-table-wrap"><table className="data-table"><thead><tr><th>Cliente</th><th>Contato</th><th>Cidade</th><th>Ciclo</th><th>Origem</th><th>Próximo contato</th></tr></thead><tbody>{clients.map(client=><tr key={client.id}><td><Link href={`/app/clientes/${client.id}`}><strong>{safeText(client.trade_name,"")||safeText(client.legal_name)}</strong></Link><br/><small>{safeText(client.legal_name)}</small></td><td>{safeText(client.email,"")}<br/><small>{safeText(client.phone,"")}</small></td><td>{[client.city,client.state].filter(Boolean).join(" / ")||"—"}</td><td><span className="badge">{safeText(client.lifecycle_stage)}</span></td><td>{safeText(client.source)}</td><td>{formatRelationshipDateTime(client.next_follow_up_at)}</td></tr>)}</tbody></table>{!clients.length?<div className="empty-state"><h3>Nenhum cliente encontrado</h3><p>Cadastre um cliente ou converta um lead qualificado.</p></div>:null}</div></section></main>;}
