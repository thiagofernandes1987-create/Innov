import{createSacTicket}from"@/app/actions/relationship";
import{RelationshipNavigation}from"@/components/relationship/relationship-navigation";
import{requireCapability}from"@/lib/authorization";
import{loadRelationshipOptions}from"@/lib/relationship/server";

export const dynamic="force-dynamic";
export default async function NewSacTicketPage({searchParams}:{searchParams:Promise<{error?:string;clientId?:string;projectId?:string}>}){await requireCapability("sac","create");const[query,options]=await Promise.all([searchParams,loadRelationshipOptions()]);return <main className="content relationship-app"><section className="page-heading"><div><span className="badge">SAC · NOVO CHAMADO</span><h1>Abrir atendimento</h1><p>Vincule o chamado ao cliente e, quando aplicável, à obra ou contrato.</p></div></section><RelationshipNavigation/>{query.error?<div className="validation blocking">{query.error}</div>:null}<form action={createSacTicket} className="card card-pad relationship-form"><div className="form-grid"><label><span>Cliente *</span><select name="clientId" required defaultValue={query.clientId??""}><option value="">Selecione</option>{options.clients.map(client=><option key={client.id} value={client.id}>{client.trade_name||client.legal_name}</option>)}</select></label><label><span>Obra</span><select name="projectId" defaultValue={query.projectId??""}><option value="">Sem obra</option>{options.projects.map(project=><option key={project.id} value={project.id}>{project.code} · {project.name}</option>)}</select></label><label><span>Categoria</span><select name="categoryId"><option value="">Atendimento geral</option>{options.categories.map(category=><option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label><span>Origem</span><select name="source"><option>INTERNAL</option><option>PHONE</option><option>EMAIL</option><option>WHATSAPP</option></select></label><label><span>Prioridade</span><select name="priority"><option>LOW</option><option selected value="NORMAL">NORMAL</option><option>HIGH</option><option>URGENT</option></select></label><label><span>ID do contrato</span><input name="contractId" placeholder="Opcional"/></label><label className="span-2"><span>Assunto *</span><input name="title" required/></label></div><label><span>Descrição detalhada *</span><textarea name="description" rows={7} required/></label><div className="form-actions"><button className="button button-primary" type="submit">Abrir chamado</button></div></form></main>;}
