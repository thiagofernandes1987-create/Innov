import Link from"next/link";
import{requireCapability}from"@/lib/authorization";
import{formatMoney}from"@/lib/financial/cash-flow";

export const dynamic="force-dynamic";
export default async function RhPayrollPage(){const context=await requireCapability("rh","read");const[{data:periods},{count:rubrics},{data:lastRun}]=await Promise.all([context.supabase.from("rh_payroll_periods").select("id,reference_month,processing_type,status,pay_date").eq("organization_id",context.organizationId).order("reference_month",{ascending:false}).limit(12),context.supabase.from("rh_rubrics").select("id",{count:"exact",head:true}).eq("organization_id",context.organizationId).eq("active",true),context.supabase.from("rh_payroll_runs").select("gross_total,deduction_total,net_total,completed_at").eq("organization_id",context.organizationId).eq("status","COMPLETED").order("completed_at",{ascending:false}).limit(1).maybeSingle()]);return <main className="content"><section className="page-heading"><div><span className="badge">RH · FOLHA</span><h1>Folha de pagamento</h1><p>Rubricas versionadas, competências, lançamentos, cálculo determinístico e fechamento.</p></div><div className="page-actions"><Link className="button button-secondary" href="/app/rh/folha/rubricas">Rubricas</Link><Link className="button button-primary" href="/app/rh/folha/competencias/nova">Abrir competência</Link></div></section><section className="stats-grid"><article className="card"><small>RUBRICAS ATIVAS</small><strong>{rubrics??0}</strong><span>códigos de cálculo</span></article><article className="card"><small>ÚLTIMO BRUTO</small><strong>{formatMoney(Number(lastRun?.gross_total??0))}</strong><span>execução concluída</span></article><article className="card"><small>ÚLTIMOS DESCONTOS</small><strong>{formatMoney(Number(lastRun?.deduction_total??0))}</strong><span>execução concluída</span></article><article className="card"><small>ÚLTIMO LÍQUIDO</small><strong>{formatMoney(Number(lastRun?.net_total??0))}</strong><span>execução concluída</span></article></section><section className="card" style={{overflowX:"auto"}}><div className="section-heading card-pad"><div><span className="eyebrow">COMPETÊNCIAS</span><h2>Processamentos recentes</h2></div><Link className="text-link" href="/app/rh/folha/competencias">Ver todas →</Link></div><table className="data-table"><thead><tr><th>Competência</th><th>Tipo</th><th>Pagamento</th><th>Status</th><th></th></tr></thead><tbody>{(periods??[]).map(period=><tr key={period.id}><td>{String(period.reference_month).slice(0,7)}</td><td>{period.processing_type}</td><td>{period.pay_date||"—"}</td><td><span className="badge">{period.status}</span></td><td><Link className="text-link" href={`/app/rh/folha/competencias/${period.id}`}>Abrir →</Link></td></tr>)}{!periods?.length?<tr><td colSpan={5}><div className="empty-state"><h3>Nenhuma competência</h3><p>Abra a primeira competência para lançar e calcular a folha.</p></div></td></tr>:null}</tbody></table></section></main>;}
