import Link from"next/link";
import{requireCapability}from"@/lib/authorization";

export const dynamic="force-dynamic";
export default async function RhPayrollPeriodsPage(){const context=await requireCapability("rh","read");const{data,error}=await context.supabase.from("rh_payroll_periods").select("id,reference_month,processing_type,pay_date,status,created_at,rh_payroll_runs(id,run_number,status,gross_total,deduction_total,net_total,completed_at)").eq("organization_id",context.organizationId).order("reference_month",{ascending:false});if(error)throw new Error(error.message);return <main className="content"><section className="page-heading"><div><span className="badge">FOLHA · COMPETÊNCIAS</span><h1>Competências e processamentos</h1><p>Cada processamento mantém entradas, execuções e fechamento próprios.</p></div><div className="page-actions"><Link className="button button-primary" href="/app/rh/folha/competencias/nova">Abrir competência</Link></div></section><section className="card" style={{overflowX:"auto"}}><table className="data-table"><thead><tr><th>Competência</th><th>Processamento</th><th>Pagamento</th><th>Execuções</th><th>Status</th><th></th></tr></thead><tbody>{(data??[]).map(period=>{const runs=Array.isArray(period.rh_payroll_runs)?period.rh_payroll_runs:[];return <tr key={period.id}><td>{String(period.reference_month).slice(0,7)}</td><td>{period.processing_type}</td><td>{period.pay_date||"—"}</td><td>{runs.length}</td><td><span className="badge">{period.status}</span></td><td><Link className="text-link" href={`/app/rh/folha/competencias/${period.id}`}>Operar →</Link></td></tr>})}{!data?.length?<tr><td colSpan={6}><div className="empty-state"><h3>Nenhuma competência aberta</h3><p>Abra uma competência mensal ou especial.</p></div></td></tr>:null}</tbody></table></section></main>;}
