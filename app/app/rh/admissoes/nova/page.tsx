import{createRhAdmissionCase}from"@/app/actions/rh-admission";
import{requireCapability}from"@/lib/authorization";
export const dynamic="force-dynamic";
export default async function NewAdmissionPage({searchParams}:{searchParams:Promise<{error?:string}>}){const context=await requireCapability("rh","create");const query=await searchParams;const[e,s,a,p,f,u,w]=await Promise.all([context.supabase.from("rh_employers").select("id,code,legal_name").eq("organization_id",context.organizationId).eq("status","ACTIVE").order("code"),context.supabase.from("rh_establishments").select("id,code,name,employer_id").eq("organization_id",context.organizationId).eq("status","ACTIVE").order("code"),context.supabase.from("rh_tax_allocations").select("id,code,name").eq("organization_id",context.organizationId).order("code"),context.supabase.from("rh_positions").select("id,code,name,cbo_code").eq("organization_id",context.organizationId).eq("active",true).order("code"),context.supabase.from("rh_functions").select("id,code,name").eq("organization_id",context.organizationId).eq("active",true).order("code"),context.supabase.from("rh_unions").select("id,code,name").eq("organization_id",context.organizationId).eq("active",true).order("code"),context.supabase.from("rh_work_schedules").select("id,code,name,weekly_hours").eq("organization_id",context.organizationId).eq("active",true).order("code")]);const err=e.error??s.error??a.error??p.error??f.error??u.error??w.error;if(err)throw new Error(err.message);return <main className="content"><section className="page-heading"><div><span className="badge">RH · NOVA ADMISSÃO</span><h1>Preparar admissão</h1><p>O vínculo só será criado depois do checklist e da ativação aprovados.</p></div></section>{query.error?<div className="validation blocking">{query.error}</div>:null}<form action={createRhAdmissionCase} className="card card-pad relationship-form"><div className="form-grid"><label className="span-2"><span>Nome completo *</span><input name="fullName" required/></label><label><span>Nome preferido</span><input name="preferredName"/></label><label><span>CPF</span><input name="cpf" inputMode="numeric"/></label><label><span>Nascimento</span><input name="birthDate" type="date"/></label><label><span>E-mail</span><input name="email" type="email"/></label><label><span>Telefone</span><input name="phone"/></label><label><span>Código trabalhador *</span><input name="workerCode" required/></label><label><span>Matrícula *</span><input name="registrationNumber" required/></label><label><span>Tipo</span><select name="employmentType" defaultValue="EMPLOYEE"><option value="EMPLOYEE">Empregado</option><option value="TSV">TSV</option><option value="APPRENTICE">Aprendiz</option><option value="INTERN">Estagiário</option><option value="DIRECTOR">Diretor</option><option value="OTHER">Outro</option></select></label><label><span>Admissão *</span><input name="admissionDate" type="date" required/></label><label><span>Empresa *</span><select name="employerId" required defaultValue=""><option value="" disabled>Selecione</option>{(e.data??[]).map(x=><option value={x.id} key={x.id}>{x.code} · {x.legal_name}</option>)}</select></label><label><span>Estabelecimento *</span><select name="establishmentId" required defaultValue=""><option value="" disabled>Selecione</option>{(s.data??[]).map(x=><option value={x.id} key={x.id}>{x.code} · {x.name}</option>)}</select></label><label><span>Lotação tributária</span><select name="taxAllocationId" defaultValue=""><option value="">Sem lotação</option>{(a.data??[]).map(x=><option value={x.id} key={x.id}>{x.code} · {x.name}</option>)}</select></label><label><span>Cargo</span><select name="positionId" defaultValue=""><option value="">Sem cargo</option>{(p.data??[]).map(x=><option value={x.id} key={x.id}>{x.code} · {x.name}{x.cbo_code?` · ${x.cbo_code}`:""}</option>)}</select></label><label><span>Função</span><select name="functionId" defaultValue=""><option value="">Sem função</option>{(f.data??[]).map(x=><option value={x.id} key={x.id}>{x.code} · {x.name}</option>)}</select></label><label><span>Sindicato</span><select name="unionId" defaultValue=""><option value="">Sem sindicato</option>{(u.data??[]).map(x=><option value={x.id} key={x.id}>{x.code} · {x.name}</option>)}</select></label><label><span>Jornada</span><select name="workScheduleId" defaultValue=""><option value="">Sem jornada</option>{(w.data??[]).map(x=><option value={x.id} key={x.id}>{x.code} · {x.name} · {x.weekly_hours}h</option>)}</select></label><label><span>Salário base *</span><input name="baseSalary" inputMode="decimal" required/></label><label><span>Estratégia eSocial</span><select name="esocialStrategy" defaultValue="S2200"><option value="S2200">S-2200</option><option value="S2190_THEN_S2200">S-2190 preliminar → S-2200</option><option value="NONE">Sem evento nesta etapa</option></select></label></div>{!(e.data?.length&&s.data?.length)?<div className="validation blocking">Cadastre empresa e estabelecimento antes de criar a admissão.</div>:null}<div className="form-actions"><button className="button button-primary" type="submit" disabled={!(e.data?.length&&s.data?.length)}>Criar caso de admissão</button></div></form></main>;}
