import Link from"next/link";
import{createInventoryItem}from"@/app/actions/inventory";
import{InventoryNavigation}from"@/components/inventory/inventory-navigation";
import{requireCapability}from"@/lib/authorization";

export const dynamic="force-dynamic";

export default async function NewInventoryItemPage({searchParams}:{searchParams:Promise<{error?:string}>}){const query=await searchParams;const context=await requireCapability("estoque","create");const[{data:units},{data:categories}]=await Promise.all([context.supabase.from("inventory_units").select("id,code,name").eq("organization_id",context.organizationId).eq("active",true).order("name"),context.supabase.from("inventory_categories").select("id,code,name").eq("organization_id",context.organizationId).eq("active",true).order("name")]);return <main className="content inventory-app"><Link className="back-link" href="/app/estoque/itens">← Itens</Link><section className="page-heading"><div><span className="badge">ESTOQUE · CATÁLOGO</span><h1>Novo item</h1><p>Defina unidade, tipo de controle, estoque mínimo e custo de referência.</p></div></section><InventoryNavigation/>{query.error&&<div className="validation blocking">{query.error}</div>}<form action={createInventoryItem} className="card card-pad inventory-form"><div className="form-grid form-grid-3"><label>Código<input name="code" required/></label><label>Nome<input name="name" required/></label><label>Tipo<select name="kind"><option value="MATERIAL">Material</option><option value="CONSUMABLE">Consumível</option><option value="TOOL">Ferramenta</option><option value="ASSET">Ativo</option></select></label><label>Categoria<select name="categoryId"><option value="">Sem categoria</option>{(categories??[]).map(category=><option key={category.id} value={category.id}>{category.code} · {category.name}</option>)}</select></label><label>Unidade<select name="unitId" required><option value="">Selecione</option>{(units??[]).map(unit=><option key={unit.id} value={unit.id}>{unit.code} · {unit.name}</option>)}</select></label><label>SKU<input name="sku"/></label><label>Código de barras<input name="barcode"/></label><label>Estoque mínimo<input type="number" name="minimumStock" min="0" step="0.0001" defaultValue="0"/></label><label>Custo de referência<input type="number" name="referenceUnitCost" min="0" step="0.0001"/></label></div><label>Descrição<textarea name="description" rows={3}/></label><label>Especificação<textarea name="specification" rows={3}/></label><div className="inventory-check-grid"><label className="checkbox-row"><input type="checkbox" name="controlsLot"/> Controla lote</label><label className="checkbox-row"><input type="checkbox" name="controlsExpiry"/> Controla validade</label><label className="checkbox-row"><input type="checkbox" name="controlsIndividualAsset"/> Controle individual por patrimônio/série</label></div><button className="button button-primary">Cadastrar item</button></form></main>;}
